# Dependencies
node_modules/

# Build outputs
dist/
build/
*.log
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

.venv-stanza/
.cache/

# Local Stanza and transformer resources. Keep the directory layout, never the
# heavyweight downloaded assets.
/backend/models/*
!/backend/models/README.md
!/backend/models/stanza/
/backend/models/stanza/*
!/backend/models/stanza/.gitkeep
!/backend/models/huggingface/
/backend/models/huggingface/*
!/backend/models/huggingface/.gitkeep

# Built language packs (publish these to a static host)
dist-packs/

# User-installed language plugin packs (runtime state; the versioned samples
# live in the engine repo). Anchored on purpose.
/backend/language-packs/
